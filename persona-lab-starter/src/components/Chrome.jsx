const nav=[['overview','Overview'],['personas','Personas'],['tasks','Tasks'],['runs','Runs'],['settings','Settings']];
export function Chrome({view,go,children}){return <div className="app-frame"><header className="topbar"><button className="wordmark" onClick={()=>go('landing')}>PERSONA LAB <span>rough draft</span></button><div className="project-chip">Acme Market / Grocery flow</div><div className="disclosure">Simulated-persona hypotheses — not validated human research</div></header><div className="app-body"><aside className="sidebar"><p>Workspace</p>{nav.map(([id,label])=><button key={id} className={view===id?'active':''} onClick={()=>go(id)}>{label}</button>)}<div className="side-note">One platform<br/>6 mapped pages<br/>3 personas</div></aside><main className="content">{children}</main></div></div>}
